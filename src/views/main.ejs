<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('partials/head') %>
    <!-- <script>
        var n;

        /* Starts table: */
        document.body.getElementById('fact').writeln("<table>");

        /* Table's head section: */
        document.body.getElementById('fact').writeln("<thead style='background-color: red; color: yellow'>");
        document.body.getElementById('fact').writeln("<tr>");
        document.body.getElementById('fact').writeln("<th>n</th>");
        document.body.getElementById('fact').writeln("<th>n!</th>");
        document.body.getElementById('fact').writeln("</tr>");
        document.body.getElementById('fact').writeln("</thead>");

        /* Table's body section: */
        document.body.getElementById('fact').writeln("<tbody>");
        for (n = 0; n <= 20; ++n) {
            if (n % 2 == 0) /* Starts new row with black background and white font: */ {
                document.body.getElementById('fact').writeln("<tr style='background-color: black; color: white;'>");
            } else /* Starts new row with blue background and yellow font: */ {
                document.body.getElementById('fact').writeln("<tr style='background-color: blue; color: yellow;'>");
            }

            /* First column of the current row: */
            document.body.getElementById('fact').writeln("<td>" + n + "</td>");

            /* Second column of the current row: */
            document.body.getElementById('fact').writeln("<td>" + factorial(n) + "</td>");

            /* Finishes current row: */
            document.body.getElementById('fact').writeln("</tr>");
        }
        document.body.getElementById('fact').writeln("</tbody>");

        /* Finishes table: */
        document.body.getElementById('fact').writeln("</table>");

        /* Calculates n!: */
        function factorial(n) {
            var counter;
            var fact = 1;

            if (n < 0) /* Invalid argument to function factorial. */ {
                return -1;
            } else {
                if (n >= 1) {
                    for (counter = 2; counter <= n; ++counter) {
                        fact *= counter;
                    }
                }

                return fact;
            }
        };
    </script> -->
    <title>Bienvenido <%= user.username%></title>
    <script src="/js/main.js" type="module"></script>
</head>

<body>
    <div class="container">
        <div class="row vh-100">
            <div class="col d-flex align-items-center justify-content-center">
                <div class="card border-primary" style="width: 25rem;">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-1"></div>
                            <div class="col-9">
                                <h4 class="card-title d-flex align-items-center flex-column">Bienvenido <%= user.username%></h4>
                            </div>
                            <div class="col-1 p-2">
                                <a href="/logout" class="btn btn-danger btn-sm">
                                    <span class="material-icons">
                                        logout
                                    </span>
                                </a>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col">

                                <div class="form-floating ">
                                    <input type="number" min="1" class="form-control" id="floatingInput"
                                        placeholder="1">
                                    <label for="floatingInput">Factorial de N</label>
                                </div>
                            </div>
                        </div>
                        <div class="row p-3">
                            <div class="col d-flex align-items-center flex-column">
                                <button class="btn btn-lg btn-primary ml-auto" id="generateTable">Calcular</button>
                            </div>
                        </div>
                        <div class="row p-1">
                            <div class="col d-flex align-items-center flex-column">
                                <table id="fact"></table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>